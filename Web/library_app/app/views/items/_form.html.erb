<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :author, "Select Author" %><br>
    <%= f.select(:author, Author.all.collect {|p| [ p.name, p.id ] }) %>
  </div>

  <div class="field">
    <%= f.label :ISBN %><br>
    <%= f.text_field :ISBN %>
  </div>
  <div class="field">
    <%= f.label :Description %><br>
    <%= f.text_field :Description %>
  </div>
    <div class="field">
    <%= f.label :avaliable, "Avaliablility" %><br>
    <%= f.check_box :avaliability %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
